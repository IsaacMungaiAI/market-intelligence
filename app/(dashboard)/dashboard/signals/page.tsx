import { DashboardPlaceholder } from "@/app/components/dashboard-placeholder";

export default function SignalsPage() {
    return (
        <DashboardPlaceholder
            description="Surface dividend alerts, momentum changes, valuation shifts, and risk events."
            label="Signals"
            title="Market intelligence signals"
        />
    );
}
